[project]
name = "rr-connect-sahayak-backend"
version = "1.0.0"
description = "Deterministic, multi-language support chatbot API (no LLM)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
    "pydantic-settings>=2.4",
]

[tool.uv]
package = false
