# No backend URL is needed anymore.
#
# The bot now runs entirely inside this Next.js app: the deterministic flow runs
# in the browser (lib/flow.ts) and the pincode -> ASM lookup is served by the
# built-in /api/asm route. There is no separate FastAPI/Render backend and no
# database to configure. This file is intentionally left without variables.
